# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.suo
*.user

# Temp
*.tmp
*.bak
*.stackdump

# Research scratch (scraped docs, search results — not project deliverables)
.firecrawl/

# ── Credentials ──
# All secrets live in config.js and nowhere else. Never commit it, and
# never paste live values into quiz-widget.html or config.example.js.
config.js
